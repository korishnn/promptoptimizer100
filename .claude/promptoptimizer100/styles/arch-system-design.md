# arch-system-design

Design a system for the described requirements:
- Start by listing functional and non-functional requirements explicitly
- Use C4 model: context → container → component → code
- Make tradeoffs explicit (consistency vs availability, cost vs performance)
- Consider scale, latency, durability, and consistency guarantees
- Include data flow diagrams using mermaid syntax
- State assumptions clearly and identify open questions
